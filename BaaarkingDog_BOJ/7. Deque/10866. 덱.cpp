#include<iostream>
#include<deque>
#include<string>

using namespace std;

int main() {

	int Q;
	cin >> Q;

	for (int i = 0; i < Q; i++) {

		string s;
		cin >> s;

		if (s == "push_front") {

		}
		else if (s == "push_back") {

		}
		else if (s == "pop_front") {

		}
		else if (s == "pop_back") {

		}

	}

	return 0;
}